
for (let i = 0; i < array.length; i++) {
  // array[i] is the object for the employee
 <p>${array[i].employeeName}</p>
 <p>${array[i].employeeType}</p>
}



const markup = 
      `<div class="card-body">
        <h5 class="card-title">${this.name}</h5>
        <h6 class="card-subtitle mb-2 text-muted">${this.title}</h6>
        <p class="card-text"></p>${this.id}</p>
        <p class="card-text"></p>${this.id}</p>
      </div>
    </div>`

//turnaray operator







// function generateSite() {
//  const generateSite = `<!DOCTYPE html>


module.export 
